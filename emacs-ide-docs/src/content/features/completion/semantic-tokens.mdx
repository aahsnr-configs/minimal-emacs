---
title: "Semantic Tokens (Semantic Highlighting)"
category: "Completion & Intelligence"
slug: "semantic-tokens"
status: "working"
vscode_parity:
  - "Type-aware syntax coloring beyond what static TextMate grammars can\n            do"
lsp_methods:
  - "textDocument/semanticTokens/full"
  - "range"
  - "delta"
routing: "eglot→eglot-semantic-tokens-mode→native font-lock / treesit"
commands:
  - action: "Toggle semantic tokens"
    cmd: "eglot-semantic-tokens-mode"
    key: "<kbd>SPC t s</kbd>"
    notes: "Enables/disables LSP semantic highlighting for the\n                        current buffer."
  - action: "Customize token faces"
    cmd: "customize-group"
    key: "<kbd>M-x customize-group RET eglot-semantic-faces</kbd>"
    notes: "Adjust colors for specific token types (e.g.,\n                        parameters, macros)."
  - action: "Toggle inlay hints (companion)"
    cmd: "eglot-inlay-hints-mode"
    key: "<kbd>SPC t h</kbd>"
    notes: "Often used alongside semantic tokens for full type-aware\n                        annotation."
parity_matrix:
  - vscode: "Type-aware syntax coloring"
    emacs: "<code>eglot-semantic-tokens-mode</code> applies faces\n                        based on LSP token types (e.g., distinguishing a\n                        <code>parameter</code> from a\n                        <code>local variable</code>)."
  - vscode: "Delta updates on edit"
    emacs: "Eglot natively requests\n                        <code>textDocument/semanticTokens/delta</code> to\n                        minimize network payload and main-thread blocking."
  - vscode: "Range requests on scroll"
    emacs: "Eglot requests\n                        <code>textDocument/semanticTokens/range</code> for\n                        visible regions only, optimizing performance in massive\n                        files."
  - vscode: "Customizable token colors"
    emacs: "<code>eglot-semantic-faces</code> customization group\n                        allows mapping specific LSP modifiers (e.g.,\n                        <code>readonly</code>, <code>deprecated</code>) to Tokyo\n                        Night palette faces."
  - vscode: "Fallback to structural highlighting"
    emacs: "If the LSP server is slow or disconnects,\n                        <code>treesit</code> (level 4) maintains perfect,\n                        zero-latency structural syntax highlighting."
config_filename: "init-semantic-tokens.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Type-aware syntax coloring`, emacs: `<code>eglot-semantic-tokens-mode</code> applies faces
                        based on LSP token types (e.g., distinguishing a
                        <code>parameter</code> from a
                        <code>local variable</code>).` },
    { vscode: `Delta updates on edit`, emacs: `Eglot natively requests
                        <code>textDocument/semanticTokens/delta</code> to
                        minimize network payload and main-thread blocking.` },
    { vscode: `Range requests on scroll`, emacs: `Eglot requests
                        <code>textDocument/semanticTokens/range</code> for
                        visible regions only, optimizing performance in massive
                        files.` },
    { vscode: `Customizable token colors`, emacs: `<code>eglot-semantic-faces</code> customization group
                        allows mapping specific LSP modifiers (e.g.,
                        <code>readonly</code>, <code>deprecated</code>) to Tokyo
                        Night palette faces.` },
    { vscode: `Fallback to structural highlighting`, emacs: `If the LSP server is slow or disconnects,
                        <code>treesit</code> (level 4) maintains perfect,
                        zero-latency structural syntax highlighting.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Toggle semantic tokens", cmd: "eglot-semantic-tokens-mode", key: `<kbd>SPC t s</kbd>`, notes: "Enables/disables LSP semantic highlighting for the
                        current buffer." },
    { action: "Customize token faces", cmd: "customize-group", key: `<kbd>M-x customize-group RET eglot-semantic-faces</kbd>`, notes: "Adjust colors for specific token types (e.g.,
                        parameters, macros)." },
    { action: "Toggle inlay hints (companion)", cmd: "eglot-inlay-hints-mode", key: `<kbd>SPC t h</kbd>`, notes: "Often used alongside semantic tokens for full type-aware
                        annotation." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

