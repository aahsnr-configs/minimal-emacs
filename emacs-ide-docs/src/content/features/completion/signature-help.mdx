---
title: "Signature Help"
category: "Completion & Intelligence"
slug: "signature-help"
status: "working"
vscode_parity:
  - "Parameter hints shown while typing inside a function call"
lsp_methods:
  - "textDocument/signatureHelp"
routing: "eglot→eglot-signature-eldoc-function→eldoc (echo area / ephemeral buffer)"
commands:
  - action: "Manual signature trigger"
    cmd: "eldoc"
    key: "<kbd>C-h .</kbd> / <kbd>K</kbd>"
    notes: "Forces atextDocument/signatureHelpquery\n                        if the automatic trigger was missed."
  - action: "Scroll long signature"
    cmd: "scroll-other-window"
    key: "<kbd>C-M-v</kbd>"
    notes: "Scrolls the*eldoc*ephemeral buffer when a\n                        signature exceeds the echo area."
  - action: "Scroll signature (back)"
    cmd: "scroll-other-window-down"
    key: "<kbd>C-M-S-v</kbd>"
    notes: "Reverse scroll for massive C++/Rust generic signatures."
  - action: "Help at point"
    cmd: "help-at-point"
    key: "<kbd>C-h .</kbd>"
    notes: "Native Emacs help surfacing that integrates with eldoc\n                        payloads."
parity_matrix:
  - vscode: "Auto-trigger on <code>(</code> or <code>,</code>"
    emacs: "<code>eglot</code> registers trigger characters via\n                        <code>textDocument/signatureHelp</code> capabilities;\n                        <code>eldoc</code> fires on\n                        <code>post-command-hook</code>."
  - vscode: "Highlights active parameter"
    emacs: "<code>eglot</code> applies\n                        <code>eldoc-highlight-function-argument</code> face to\n                        the active parameter index."
  - vscode: "Cycles through overloads"
    emacs: "<code>eldoc</code> natively supports multiple\n                        signatures; <kbd>C-h .</kbd> or arrow keys can cycle if\n                        the server returns an array of signatures."
  - vscode: "Floating tooltip for long signatures"
    emacs: "Emacs 31\n                        <code>eldoc-echo-area-prefer-doc-buffer t</code> routes\n                        long signatures to the <code>*eldoc*</code> buffer\n                        without shifting window layouts."
  - vscode: "Manual trigger shortcut"
    emacs: "<kbd>C-h .</kbd> (<code>help-at-point</code>) or\n                        <kbd>K</kbd> (<code>eldoc</code>)."
  - vscode: "Dismiss on cursor move"
    emacs: "<code>eldoc</code> automatically clears the echo area or\n                        hides the ephemeral buffer when the cursor leaves the\n                        callable scope."
config_filename: "init-signature-help.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Auto-trigger on <code>(</code> or <code>,</code>`, emacs: `<code>eglot</code> registers trigger characters via
                        <code>textDocument/signatureHelp</code> capabilities;
                        <code>eldoc</code> fires on
                        <code>post-command-hook</code>.` },
    { vscode: `Highlights active parameter`, emacs: `<code>eglot</code> applies
                        <code>eldoc-highlight-function-argument</code> face to
                        the active parameter index.` },
    { vscode: `Cycles through overloads`, emacs: `<code>eldoc</code> natively supports multiple
                        signatures; <kbd>C-h .</kbd> or arrow keys can cycle if
                        the server returns an array of signatures.` },
    { vscode: `Floating tooltip for long signatures`, emacs: `Emacs 31
                        <code>eldoc-echo-area-prefer-doc-buffer t</code> routes
                        long signatures to the <code>*eldoc*</code> buffer
                        without shifting window layouts.` },
    { vscode: `Manual trigger shortcut`, emacs: `<kbd>C-h .</kbd> (<code>help-at-point</code>) or
                        <kbd>K</kbd> (<code>eldoc</code>).` },
    { vscode: `Dismiss on cursor move`, emacs: `<code>eldoc</code> automatically clears the echo area or
                        hides the ephemeral buffer when the cursor leaves the
                        callable scope.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Manual signature trigger", cmd: "eldoc", key: `<kbd>C-h .</kbd> / <kbd>K</kbd>`, notes: "Forces atextDocument/signatureHelpquery
                        if the automatic trigger was missed." },
    { action: "Scroll long signature", cmd: "scroll-other-window", key: `<kbd>C-M-v</kbd>`, notes: "Scrolls the*eldoc*ephemeral buffer when a
                        signature exceeds the echo area." },
    { action: "Scroll signature (back)", cmd: "scroll-other-window-down", key: `<kbd>C-M-S-v</kbd>`, notes: "Reverse scroll for massive C++/Rust generic signatures." },
    { action: "Help at point", cmd: "help-at-point", key: `<kbd>C-h .</kbd>`, notes: "Native Emacs help surfacing that integrates with eldoc
                        payloads." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

