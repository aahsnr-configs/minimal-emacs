---
title: "IntelliSense / Code Completion"
category: "Completion & Intelligence"
slug: "intellisense"
status: "working"
vscode_parity:
  - "Autocomplete popup"
  - "ghost text"
  - "auto-imports"
  - "fuzzy\n            filtering"
lsp_methods:
  - "textDocument/completion"
  - "completionItem/resolve"
routing: "eglot→capf→cape→corfu"
commands:
  - action: "Trigger completion"
    cmd: "completion-at-point"
    key: "<kbd>TAB</kbd> / <kbd>C-SPC</kbd>"
    notes: "Indents if unaligned, triggers popup if aligned."
  - action: "Next candidate"
    cmd: "corfu-next"
    key: "<kbd>TAB</kbd> / <kbd>C-n</kbd>"
    notes: "Cycles forward."
  - action: "Previous candidate"
    cmd: "corfu-previous"
    key: "<kbd>S-TAB</kbd> / <kbd>C-p</kbd>"
    notes: "Cycles backward."
  - action: "Insert candidate"
    cmd: "corfu-insert"
    key: "<kbd>RET</kbd>"
    notes: "Commits, triggering auto-imports via eglot."
  - action: "Toggle documentation"
    cmd: "corfu-popupinfo-toggle"
    key: "<kbd>M-h</kbd>"
    notes: "Shows/hides completionItem/resolve docstring."
  - action: "Quick select (insert)"
    cmd: "corfu-quick-insert"
    key: "<kbd>M-q</kbd>"
    notes: "Avy-style 1-2 char jump to insert candidate."
  - action: "Exit completion"
    cmd: "corfu-quit"
    key: "<kbd>ESC</kbd> / <kbd>C-g</kbd>"
    notes: "Cancels popup without inserting."
parity_matrix:
  - vscode: "Autocomplete popup on typing"
    emacs: "<code>corfu-auto t</code> with\n                        <code>corfu-auto-delay 0.2</code>"
  - vscode: "Ghost text (inline preview)"
    emacs: "<code>corfu-candidate-overlay-mode</code> renders dimmed\n                        text in-buffer"
  - vscode: "Auto-import on accept"
    emacs: "<code>eglot</code> processes\n                        <code>additionalTextEdits</code> on\n                        <code>corfu-insert</code>"
  - vscode: "Fuzzy / Substring filtering"
    emacs: "<code>orderless</code> matching styles (space-separated\n                        components)"
  - vscode: "Snippet integration"
    emacs: "<code>yasnippet-capf</code> merged into pipeline via\n                        <code>cape</code>"
  - vscode: "Documentation on hover"
    emacs: "<code>corfu-popupinfo</code> (<kbd>M-h</kbd>) triggers\n                        <code>completionItem/resolve</code>"
  - vscode: "Icons in autocomplete list"
    emacs: "<code>nerd-icons-corfu</code> injects glyphs into the\n                        Corfu margin"
  - vscode: "Dismiss on Escape"
    emacs: "<code>corfu-quit</code> bound to <kbd>ESC</kbd> in\n                        <code>corfu-map</code>"
config_filename: "init-completion.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Autocomplete popup on typing`, emacs: `<code>corfu-auto t</code> with
                        <code>corfu-auto-delay 0.2</code>` },
    { vscode: `Ghost text (inline preview)`, emacs: `<code>corfu-candidate-overlay-mode</code> renders dimmed
                        text in-buffer` },
    { vscode: `Auto-import on accept`, emacs: `<code>eglot</code> processes
                        <code>additionalTextEdits</code> on
                        <code>corfu-insert</code>` },
    { vscode: `Fuzzy / Substring filtering`, emacs: `<code>orderless</code> matching styles (space-separated
                        components)` },
    { vscode: `Snippet integration`, emacs: `<code>yasnippet-capf</code> merged into pipeline via
                        <code>cape</code>` },
    { vscode: `Documentation on hover`, emacs: `<code>corfu-popupinfo</code> (<kbd>M-h</kbd>) triggers
                        <code>completionItem/resolve</code>` },
    { vscode: `Icons in autocomplete list`, emacs: `<code>nerd-icons-corfu</code> injects glyphs into the
                        Corfu margin` },
    { vscode: `Dismiss on Escape`, emacs: `<code>corfu-quit</code> bound to <kbd>ESC</kbd> in
                        <code>corfu-map</code>` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Trigger completion", cmd: "completion-at-point", key: `<kbd>TAB</kbd> / <kbd>C-SPC</kbd>`, notes: "Indents if unaligned, triggers popup if aligned." },
    { action: "Next candidate", cmd: "corfu-next", key: `<kbd>TAB</kbd> / <kbd>C-n</kbd>`, notes: "Cycles forward." },
    { action: "Previous candidate", cmd: "corfu-previous", key: `<kbd>S-TAB</kbd> / <kbd>C-p</kbd>`, notes: "Cycles backward." },
    { action: "Insert candidate", cmd: "corfu-insert", key: `<kbd>RET</kbd>`, notes: "Commits, triggering auto-imports via eglot." },
    { action: "Toggle documentation", cmd: "corfu-popupinfo-toggle", key: `<kbd>M-h</kbd>`, notes: "Shows/hides completionItem/resolve docstring." },
    { action: "Quick select (insert)", cmd: "corfu-quick-insert", key: `<kbd>M-q</kbd>`, notes: "Avy-style 1-2 char jump to insert candidate." },
    { action: "Exit completion", cmd: "corfu-quit", key: `<kbd>ESC</kbd> / <kbd>C-g</kbd>`, notes: "Cancels popup without inserting." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

