---
title: "Hover Info (Childframe Parity)"
category: "Completion & Intelligence"
slug: "hover-info-childframe"
status: "working"
vscode_parity:
  - "Floating tooltip with rich markdown, type info, and\n            signatures"
lsp_methods:
  - "textDocument/hover"
routing: "eglot→eldoc→eldoc-box (childframe) OR *eldoc* buffer (TTY)"
commands:
  - action: "Hover at point (keyboard)"
    cmd: "eldoc"
    key: "<kbd>K</kbd> (Evil normal)"
    notes: "TriggerstextDocument/hoverand spawns the\n                        childframe."
  - action: "Help at point"
    cmd: "help-at-point"
    key: "<kbd>C-h .</kbd>"
    notes: "Native Emacs help surfacing viaeldoc-help-at-pt."
  - action: "Scroll hover tooltip"
    cmd: "eldoc-box-scroll-up/down"
    key: "<kbd>C-M-v</kbd> / <kbd>C-M-S-v</kbd>"
    notes: "Scrolls the childframe when docstrings exceed the\n                        viewport."
  - action: "Toggle candidate docs"
    cmd: "corfu-popupinfo-toggle"
    key: "<kbd>M-h</kbd> (in <code>corfu-map</code>)"
    notes: "Shows/hides childframe docs for the active completion\n                        candidate."
parity_matrix:
  - vscode: "Floating tooltip on cursor idle"
    emacs: "<code>eldoc-box-hover-at-point-mode</code> spawns\n                        childframe after <code>eldoc-idle-delay</code>."
  - vscode: "Rich markdown rendering"
    emacs: "<code>markdown-ts-mode</code> fontifies code blocks\n                        inside the <code>eldoc-box</code> childframe."
  - vscode: "Tooltip disappears on cursor move"
    emacs: "<code>eldoc-box-clear-after-use t</code> destroys the\n                        childframe instantly."
  - vscode: "Single-line hints in status bar"
    emacs: "<code>eldoc-box-only-multi-line t</code> keeps 1-liners\n                        in the echo area."
  - vscode: "Scroll long documentation"
    emacs: "<kbd>C-M-v</kbd> / <kbd>C-M-S-v</kbd> scrolls the\n                        <code>eldoc-box</code> childframe window."
  - vscode: "Hover on completion candidate"
    emacs: "<code>corfu-popupinfo-toggle</code> (<kbd>M-h</kbd>)\n                        spawns a childframe for\n                        <code>completionItem/resolve</code>."
  - vscode: "Works over SSH / Terminal"
    emacs: "Emacs 31\n                        <code>eldoc-echo-area-prefer-doc-buffer</code> routes to\n                        <code>*eldoc*</code> buffer natively."
config_filename: "init-hover-childframe.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Floating tooltip on cursor idle`, emacs: `<code>eldoc-box-hover-at-point-mode</code> spawns
                        childframe after <code>eldoc-idle-delay</code>.` },
    { vscode: `Rich markdown rendering`, emacs: `<code>markdown-ts-mode</code> fontifies code blocks
                        inside the <code>eldoc-box</code> childframe.` },
    { vscode: `Tooltip disappears on cursor move`, emacs: `<code>eldoc-box-clear-after-use t</code> destroys the
                        childframe instantly.` },
    { vscode: `Single-line hints in status bar`, emacs: `<code>eldoc-box-only-multi-line t</code> keeps 1-liners
                        in the echo area.` },
    { vscode: `Scroll long documentation`, emacs: `<kbd>C-M-v</kbd> / <kbd>C-M-S-v</kbd> scrolls the
                        <code>eldoc-box</code> childframe window.` },
    { vscode: `Hover on completion candidate`, emacs: `<code>corfu-popupinfo-toggle</code> (<kbd>M-h</kbd>)
                        spawns a childframe for
                        <code>completionItem/resolve</code>.` },
    { vscode: `Works over SSH / Terminal`, emacs: `Emacs 31
                        <code>eldoc-echo-area-prefer-doc-buffer</code> routes to
                        <code>*eldoc*</code> buffer natively.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Hover at point (keyboard)", cmd: "eldoc", key: `<kbd>K</kbd> (Evil normal)`, notes: "TriggerstextDocument/hoverand spawns the
                        childframe." },
    { action: "Help at point", cmd: "help-at-point", key: `<kbd>C-h .</kbd>`, notes: "Native Emacs help surfacing viaeldoc-help-at-pt." },
    { action: "Scroll hover tooltip", cmd: "eldoc-box-scroll-up/down", key: `<kbd>C-M-v</kbd> / <kbd>C-M-S-v</kbd>`, notes: "Scrolls the childframe when docstrings exceed the
                        viewport." },
    { action: "Toggle candidate docs", cmd: "corfu-popupinfo-toggle", key: `<kbd>M-h</kbd> (in <code>corfu-map</code>)`, notes: "Shows/hides childframe docs for the active completion
                        candidate." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

