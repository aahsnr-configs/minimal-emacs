---
title: "Workspace File-Operation Hooks"
category: "Workspace & Files"
slug: "file-operation-hooks"
status: "working"
vscode_parity:
  - "Auto-updating imports and references when renaming or deleting\n            files via the file explorer"
lsp_methods:
  - "workspace/willRenameFiles"
  - "workspace/didRenameFiles"
routing: "Custom Elisp→eglot--execute-request→eglot--apply-workspace-edit→OS\n              rename-file"
commands:
  - action: "Rename file with LSP updates"
    cmd: "ar/eglot-rename-file"
    key: "<kbd>SPC f R</kbd>"
    notes: "Prompts for new name, updates imports across the\n                        project, and renames the file."
  - action: "Native VC rename (fallback)"
    cmd: "vc-rename-file"
    key: "<kbd>C-x v R</kbd>"
    notes: "Renames the file and updates VCS. Advised to trigger LSP\n                        updates automatically."
parity_matrix:
  - vscode: "Rename file in Explorer updates imports"
    emacs: "<code>ar/eglot-rename-file</code> sends\n                        <code>willRenameFiles</code>, applies edits, then\n                        renames."
  - vscode: "Rename file in Explorer updates VCS"
    emacs: "Advising or chaining with <code>vc-rename-file</code>\n                        ensures Git tracks the move."
  - vscode: "Delete file updates references"
    emacs: "Similar custom wrapper can be built for\n                        <code>workspace/willDeleteFiles</code>."
  - vscode: "No manual \"Find and Replace\" needed"
    emacs: "The LSP server calculates the exact AST-aware import\n                        paths to update."
  - vscode: "Buffer automatically visits new file"
    emacs: "<code>set-visited-file-name</code> seamlessly\n                        transitions the current buffer to the new path."
config_filename: "init-file-operations.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Rename file in Explorer updates imports`, emacs: `<code>ar/eglot-rename-file</code> sends
                        <code>willRenameFiles</code>, applies edits, then
                        renames.` },
    { vscode: `Rename file in Explorer updates VCS`, emacs: `Advising or chaining with <code>vc-rename-file</code>
                        ensures Git tracks the move.` },
    { vscode: `Delete file updates references`, emacs: `Similar custom wrapper can be built for
                        <code>workspace/willDeleteFiles</code>.` },
    { vscode: `No manual "Find and Replace" needed`, emacs: `The LSP server calculates the exact AST-aware import
                        paths to update.` },
    { vscode: `Buffer automatically visits new file`, emacs: `<code>set-visited-file-name</code> seamlessly
                        transitions the current buffer to the new path.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Rename file with LSP updates", cmd: "ar/eglot-rename-file", key: `<kbd>SPC f R</kbd>`, notes: "Prompts for new name, updates imports across the
                        project, and renames the file." },
    { action: "Native VC rename (fallback)", cmd: "vc-rename-file", key: `<kbd>C-x v R</kbd>`, notes: "Renames the file and updates VCS. Advised to trigger LSP
                        updates automatically." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

