---
title: "Quick Fix Lightbulb"
category: "Code Actions & Refactoring"
slug: "quick-fix-lightbulb"
status: "working"
vscode_parity:
  - "Gutter lightbulb icon indicating available code actions or quick\n            fixes"
lsp_methods:
  - "textDocument/codeAction"
routing: "eglot→eglot-code-action-indications→margin/eldoc-hint rendering"
commands:
  - action: "Invoke code actions"
    cmd: "eglot-code-actions"
    key: "<kbd>SPC c a</kbd> / <kbd>C-.</kbd>"
    notes: "Opens the searchable menu of all available actions at\n                        point."
  - action: "Toggle margin indicator"
    cmd: "eglot-code-action-indications"
    key: "—"
    notes: "Configured viasetqto show inmargin,eldoc-hint, or both."
parity_matrix:
  - vscode: "Lightbulb icon appears in gutter"
    emacs: "<code>eglot-code-action-indications</code> set to\n                        <code>margin</code> renders the indicator in the left\n                        fringe."
  - vscode: "Indicator disappears when no actions exist"
    emacs: "<code>eglot</code> automatically clears the margin\n                        indicator when <code>textDocument/codeAction</code>\n                        returns empty."
  - vscode: "Click indicator to open actions"
    emacs: "Emacs 31 <code>eglot-code-actions-at-mouse</code> allows\n                        invoking the menu by clicking the diagnostic or\n                        indicator."
  - vscode: "Fallback hint in status area"
    emacs: "<code>eldoc-hint</code> in\n                        <code>eglot-code-action-indications</code> surfaces a\n                        subtle hint in the echo area."
  - vscode: "No lag or stutter while typing"
    emacs: "Idle delay and debouncing prevent the LSP server from\n                        being spammed on every keystroke."
config_filename: "init-quick-fix-lightbulb.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Lightbulb icon appears in gutter`, emacs: `<code>eglot-code-action-indications</code> set to
                        <code>margin</code> renders the indicator in the left
                        fringe.` },
    { vscode: `Indicator disappears when no actions exist`, emacs: `<code>eglot</code> automatically clears the margin
                        indicator when <code>textDocument/codeAction</code>
                        returns empty.` },
    { vscode: `Click indicator to open actions`, emacs: `Emacs 31 <code>eglot-code-actions-at-mouse</code> allows
                        invoking the menu by clicking the diagnostic or
                        indicator.` },
    { vscode: `Fallback hint in status area`, emacs: `<code>eldoc-hint</code> in
                        <code>eglot-code-action-indications</code> surfaces a
                        subtle hint in the echo area.` },
    { vscode: `No lag or stutter while typing`, emacs: `Idle delay and debouncing prevent the LSP server from
                        being spammed on every keystroke.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Invoke code actions", cmd: "eglot-code-actions", key: `<kbd>SPC c a</kbd> / <kbd>C-.</kbd>`, notes: "Opens the searchable menu of all available actions at
                        point." },
    { action: "Toggle margin indicator", cmd: "eglot-code-action-indications", key: `—`, notes: "Configured viasetqto show inmargin,eldoc-hint, or both." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

