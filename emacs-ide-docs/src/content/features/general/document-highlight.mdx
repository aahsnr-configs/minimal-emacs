---
title: "Document Highlight"
category: "Navigation & Visual Enhancements"
slug: "document-highlight"
status: "working"
vscode_parity:
  - "Auto-highlighting of all references to the symbol at the cursor\n            position"
lsp_methods:
  - "textDocument/documentHighlight"
routing: "eglot→:documentHighlightProvider→native overlay application"
commands:
  - action: "Cycle through highlights"
    cmd: "xref-find-references"
    key: "<kbd>M-?</kbd>"
    notes: "If more context is needed, jump to the full reference\n                        list."
  - action: "Jump to next occurrence"
    cmd: "isearch-forward-symbol-at-point"
    key: "<kbd>M-s .</kbd>"
    notes: "Native Emacs fallback to cycle through local occurrences\n                        if LSP is disconnected."
parity_matrix:
  - vscode: "Auto-highlights symbol on cursor stop"
    emacs: "<code>eglot</code> natively triggers\n                        <code>textDocument/documentHighlight</code> on cursor\n                        idle."
  - vscode: "Highlights read/write occurrences differently"
    emacs: "<code>eglot</code> parses the <code>kind</code>\n                        (read/write/text) from the LSP response and applies\n                        distinct faces."
  - vscode: "Highlight clears on cursor move"
    emacs: "Overlays are automatically destroyed when the cursor\n                        moves to a new symbol or buffer."
  - vscode: "Works across the entire visible buffer"
    emacs: "<code>eglot</code> requests highlights for the current\n                        file scope and renders them as buffer overlays."
  - vscode: "Fallback when LSP is slow/disconnected"
    emacs: "Native\n                        <code>isearch-forward-symbol-at-point</code> (<kbd>M-s .</kbd>) provides instant local regex-based highlighting."
config_filename: "init-highlight.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Auto-highlights symbol on cursor stop`, emacs: `<code>eglot</code> natively triggers
                        <code>textDocument/documentHighlight</code> on cursor
                        idle.` },
    { vscode: `Highlights read/write occurrences differently`, emacs: `<code>eglot</code> parses the <code>kind</code>
                        (read/write/text) from the LSP response and applies
                        distinct faces.` },
    { vscode: `Highlight clears on cursor move`, emacs: `Overlays are automatically destroyed when the cursor
                        moves to a new symbol or buffer.` },
    { vscode: `Works across the entire visible buffer`, emacs: `<code>eglot</code> requests highlights for the current
                        file scope and renders them as buffer overlays.` },
    { vscode: `Fallback when LSP is slow/disconnected`, emacs: `Native
                        <code>isearch-forward-symbol-at-point</code> (<kbd>M-s .</kbd>) provides instant local regex-based highlighting.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Cycle through highlights", cmd: "xref-find-references", key: `<kbd>M-?</kbd>`, notes: "If more context is needed, jump to the full reference
                        list." },
    { action: "Jump to next occurrence", cmd: "isearch-forward-symbol-at-point", key: `<kbd>M-s .</kbd>`, notes: "Native Emacs fallback to cycle through local occurrences
                        if LSP is disconnected." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

