---
title: "Linked Editing Range (Auto Rename Tag)"
category: "Visual Enhancements / Editing"
slug: "linked-editing-range"
status: "working"
vscode_parity:
  - "Editing one HTML/JSX tag name automatically updates its matching\n            pair"
lsp_methods:
  - "textDocument/linkedEditingRange"
routing: "auto-rename-tag→native Emacs\n              regex/AST parsing"
commands:
  - action: "Toggle auto-rename"
    cmd: "auto-rename-tag-mode"
    key: "—"
    notes: "Enabled automatically via mode hooks for relevant\n                        languages."
  - action: "Manual tag jump (fallback)"
    cmd: "sgml-skip-tag-forward/backward"
    key: "<kbd>C-c C-f</kbd> / <kbd>C-c C-b</kbd>"
    notes: "Native Emacs commands to manually jump between paired\n                        tags if needed."
parity_matrix:
  - vscode: "Typing in <code><div></code> instantly updates\n                        <code></div></code>"
    emacs: "<code>auto-rename-tag-mode</code> detects the pair and\n                        updates it synchronously with 0ms latency."
  - vscode: "Works for HTML, XML, and JSX/TSX"
    emacs: "Hooked into <code>html-ts-mode</code>,\n                        <code>nxml-mode</code>, and <code>tsx-ts-mode</code>\n                        respectively."
  - vscode: "No lag or stutter while typing"
    emacs: "Native local execution completely bypasses the LSP\n                        network request cycle."
  - vscode: "Respects cursor position"
    emacs: "The package is designed to adjust the cursor position\n                        intelligently after the rename operation, preventing\n                        spatial drift."
config_filename: "init-linked-editing.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Typing in <code><div></code> instantly updates
                        <code></div></code>`, emacs: `<code>auto-rename-tag-mode</code> detects the pair and
                        updates it synchronously with 0ms latency.` },
    { vscode: `Works for HTML, XML, and JSX/TSX`, emacs: `Hooked into <code>html-ts-mode</code>,
                        <code>nxml-mode</code>, and <code>tsx-ts-mode</code>
                        respectively.` },
    { vscode: `No lag or stutter while typing`, emacs: `Native local execution completely bypasses the LSP
                        network request cycle.` },
    { vscode: `Respects cursor position`, emacs: `The package is designed to adjust the cursor position
                        intelligently after the rename operation, preventing
                        spatial drift.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Toggle auto-rename", cmd: "auto-rename-tag-mode", key: `—`, notes: "Enabled automatically via mode hooks for relevant
                        languages." },
    { action: "Manual tag jump (fallback)", cmd: "sgml-skip-tag-forward/backward", key: `<kbd>C-c C-f</kbd> / <kbd>C-c C-b</kbd>`, notes: "Native Emacs commands to manually jump between paired
                        tags if needed." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

