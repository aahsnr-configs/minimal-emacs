---
title: "Inline Values (Debugger)"
category: "Diagnostics & Debugging"
slug: "inline-values"
status: "working"
vscode_parity:
  - "Inline variable value overlays displayed at the end of the line\n            when execution is paused"
routing: "dape→dape-inline-variables→native buffer overlays"
commands:
  - action: "Toggle inline variables"
    cmd: "dape-toggle-inline-variables"
    key: "—"
    notes: "Toggles the overlay visibility dynamically."
  - action: "Continue execution"
    cmd: "dape-continue"
    key: "<kbd>F5</kbd> / <kbd>SPC d c</kbd>"
    notes: "Resumes execution; inline overlays automatically clear."
  - action: "Step over"
    cmd: "dape-next"
    key: "<kbd>F10</kbd> / <kbd>SPC d n</kbd>"
    notes: "Steps to the next line; overlays update with new\n                        variable states."
  - action: "Step into"
    cmd: "dape-step-in"
    key: "<kbd>F11</kbd> / <kbd>SPC d i</kbd>"
    notes: "Steps into the current function call."
  - action: "Evaluate expression"
    cmd: "dape-evaluate"
    key: "<kbd>SPC d e</kbd>"
    notes: "Opens minibuffer to evaluate and temporarily display an\n                        expression."
parity_matrix:
  - vscode: "Inline grey text showing variable values when paused"
    emacs: "<code>dape-inline-variables t</code> renders\n                        <code>dape-inline-value-face</code> overlays at the end\n                        of the line."
  - vscode: "Overlays update automatically on step"
    emacs: "<code>dape-next</code> / <code>dape-step-in</code>\n                        triggers an overlay refresh with the new scope's\n                        variable state."
  - vscode: "Overlays clear when continuing"
    emacs: "<code>dape-continue</code> automatically removes all\n                        inline value overlays."
  - vscode: "Click to evaluate or expand"
    emacs: "<code>dape</code> overlays can be configured to trigger\n                        <code>dape-evaluate</code> on mouse interaction (or via\n                        <kbd>SPC d e</kbd>)."
  - vscode: "Respects editor theme"
    emacs: "<code>dape-inline-value-face</code> is customized to\n                        inherit <code>shadow</code> with Tokyo Night accent\n                        colors."
config_filename: "init-inline-values.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `Inline grey text showing variable values when paused`, emacs: `<code>dape-inline-variables t</code> renders
                        <code>dape-inline-value-face</code> overlays at the end
                        of the line.` },
    { vscode: `Overlays update automatically on step`, emacs: `<code>dape-next</code> / <code>dape-step-in</code>
                        triggers an overlay refresh with the new scope's
                        variable state.` },
    { vscode: `Overlays clear when continuing`, emacs: `<code>dape-continue</code> automatically removes all
                        inline value overlays.` },
    { vscode: `Click to evaluate or expand`, emacs: `<code>dape</code> overlays can be configured to trigger
                        <code>dape-evaluate</code> on mouse interaction (or via
                        <kbd>SPC d e</kbd>).` },
    { vscode: `Respects editor theme`, emacs: `<code>dape-inline-value-face</code> is customized to
                        inherit <code>shadow</code> with Tokyo Night accent
                        colors.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Toggle inline variables", cmd: "dape-toggle-inline-variables", key: `—`, notes: "Toggles the overlay visibility dynamically." },
    { action: "Continue execution", cmd: "dape-continue", key: `<kbd>F5</kbd> / <kbd>SPC d c</kbd>`, notes: "Resumes execution; inline overlays automatically clear." },
    { action: "Step over", cmd: "dape-next", key: `<kbd>F10</kbd> / <kbd>SPC d n</kbd>`, notes: "Steps to the next line; overlays update with new
                        variable states." },
    { action: "Step into", cmd: "dape-step-in", key: `<kbd>F11</kbd> / <kbd>SPC d i</kbd>`, notes: "Steps into the current function call." },
    { action: "Evaluate expression", cmd: "dape-evaluate", key: `<kbd>SPC d e</kbd>`, notes: "Opens minibuffer to evaluate and temporarily display an
                        expression." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

