---
title: "Code Actions (Quick Fixes & Refactorings)"
category: "Code Actions & Refactoring"
slug: "code-actions"
status: "working"
vscode_parity:
  - "Ctrl+. lightbulb menu, inline quick fixes, and refactoring\n            options"
lsp_methods:
  - "textDocument/codeAction"
  - "codeAction/resolve"
routing: "eglot→eglot-code-actions→completing-read (Vertico/Consult)"
commands:
  - action: "Invoke code actions"
    cmd: "eglot-code-actions"
    key: "<kbd>SPC c a</kbd> / <kbd>C-.</kbd>"
    notes: "Opens the searchable menu of all available actions at\n                        point."
  - action: "Organize imports"
    cmd: "eglot-code-action-organize-imports"
    key: "<kbd>SPC c i</kbd>"
    notes: "Direct shortcut for the most common source action."
  - action: "Quick fix (diagnostic)"
    cmd: "eglot-code-action-quickfix"
    key: "—"
    notes: "Filters the code action menu to show only quick fixes\n                        for the current diagnostic."
  - action: "Execute server command"
    cmd: "eglot-execute-command"
    key: "<kbd>SPC c x</kbd>"
    notes: "Runs workspace-level commands surfaced by the server\n                        (e.g., \"Restart Server\")."
  - action: "Toggle lightbulb indicator"
    cmd: "eglot-code-action-indications"
    key: "—"
    notes: "Configured viasetqto show inmargin,eldoc-hint, or both."
parity_matrix:
  - vscode: "<kbd>Ctrl+.</kbd> opens quick fix menu"
    emacs: "<kbd>SPC c a</kbd> (<code>eglot-code-actions</code>)\n                        opens Vertico-powered action menu."
  - vscode: "Lightbulb icon in gutter"
    emacs: "Emacs 31 <code>eglot-code-action-indications</code>\n                        renders 💡 in the left margin."
  - vscode: "\"Organize Imports\" shortcut"
    emacs: "<kbd>SPC c i</kbd>\n                        (<code>eglot-code-action-organize-imports</code>)."
  - vscode: "Action menu filters by type (Quick Fix, Refactor)"
    emacs: "<code>vertico</code> allows instant fuzzy filtering\n                        (e.g., typing <code>fix</code> or\n                        <code>refactor</code>)."
  - vscode: "Live preview of the proposed change"
    emacs: "<code>consult</code> integration provides live buffer\n                        preview of the action's effect."
  - vscode: "Click lightbulb to open actions"
    emacs: "Emacs 31 <code>eglot-code-actions-at-mouse</code> (click\n                        diagnostic with <kbd>mouse-2</kbd>)."
  - vscode: "Workspace-level commands"
    emacs: "<kbd>SPC c x</kbd>\n                        (<code>eglot-execute-command</code>) surfaces\n                        server-defined palette entries."
config_filename: "init-code-actions.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>Ctrl+.</kbd> opens quick fix menu`, emacs: `<kbd>SPC c a</kbd> (<code>eglot-code-actions</code>)
                        opens Vertico-powered action menu.` },
    { vscode: `Lightbulb icon in gutter`, emacs: `Emacs 31 <code>eglot-code-action-indications</code>
                        renders 💡 in the left margin.` },
    { vscode: `"Organize Imports" shortcut`, emacs: `<kbd>SPC c i</kbd>
                        (<code>eglot-code-action-organize-imports</code>).` },
    { vscode: `Action menu filters by type (Quick Fix, Refactor)`, emacs: `<code>vertico</code> allows instant fuzzy filtering
                        (e.g., typing <code>fix</code> or
                        <code>refactor</code>).` },
    { vscode: `Live preview of the proposed change`, emacs: `<code>consult</code> integration provides live buffer
                        preview of the action's effect.` },
    { vscode: `Click lightbulb to open actions`, emacs: `Emacs 31 <code>eglot-code-actions-at-mouse</code> (click
                        diagnostic with <kbd>mouse-2</kbd>).` },
    { vscode: `Workspace-level commands`, emacs: `<kbd>SPC c x</kbd>
                        (<code>eglot-execute-command</code>) surfaces
                        server-defined palette entries.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Invoke code actions", cmd: "eglot-code-actions", key: `<kbd>SPC c a</kbd> / <kbd>C-.</kbd>`, notes: "Opens the searchable menu of all available actions at
                        point." },
    { action: "Organize imports", cmd: "eglot-code-action-organize-imports", key: `<kbd>SPC c i</kbd>`, notes: "Direct shortcut for the most common source action." },
    { action: "Quick fix (diagnostic)", cmd: "eglot-code-action-quickfix", key: `—`, notes: "Filters the code action menu to show only quick fixes
                        for the current diagnostic." },
    { action: "Execute server command", cmd: "eglot-execute-command", key: `<kbd>SPC c x</kbd>`, notes: "Runs workspace-level commands surfaced by the server
                        (e.g., "Restart Server")." },
    { action: "Toggle lightbulb indicator", cmd: "eglot-code-action-indications", key: `—`, notes: "Configured viasetqto show inmargin,eldoc-hint, or both." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

