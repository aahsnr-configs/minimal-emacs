---
title: "Rename Symbol"
category: "Code Actions & Refactoring"
slug: "rename-symbol"
status: "working"
vscode_parity:
  - "F2 (Rename) across the entire workspace"
lsp_methods:
  - "textDocument/prepareRename"
  - "textDocument/rename"
routing: "eglot→eglot-rename→workspace/applyEdit"
commands:
  - action: "Rename symbol"
    cmd: "eglot-rename"
    key: "<kbd>F2</kbd> / <kbd>SPC c r</kbd>"
    notes: "Prompts for new name and applies project-wide rename."
  - action: "Prepare rename (internal)"
    cmd: "eglot--prepare-rename"
    key: "—"
    notes: "Automatically invoked byeglot-renameto\n                        check if the symbol is valid for renaming."
parity_matrix:
  - vscode: "<kbd>F2</kbd> prompts for new name"
    emacs: "<kbd>F2</kbd> or <kbd>SPC c r</kbd> invokes\n                        <code>eglot-rename</code> with <code>read-string</code>."
  - vscode: "Validates rename target first"
    emacs: "<code>eglot</code> automatically sends\n                        <code>textDocument/prepareRename</code> before\n                        prompting."
  - vscode: "Renames across all project files"
    emacs: "<code>eglot</code> processes the\n                        <code>workspace/applyEdit</code> payload, opening and\n                        modifying files as needed."
  - vscode: "Preserves undo history"
    emacs: "Native Emacs undo handles the multi-file edits cleanly."
  - vscode: "Fails gracefully on invalid targets"
    emacs: "<code>eglot</code> displays a user-friendly error if\n                        <code>prepareRename</code> rejects the location (e.g.,\n                        renaming a keyword)."
config_filename: "init-rename.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>F2</kbd> prompts for new name`, emacs: `<kbd>F2</kbd> or <kbd>SPC c r</kbd> invokes
                        <code>eglot-rename</code> with <code>read-string</code>.` },
    { vscode: `Validates rename target first`, emacs: `<code>eglot</code> automatically sends
                        <code>textDocument/prepareRename</code> before
                        prompting.` },
    { vscode: `Renames across all project files`, emacs: `<code>eglot</code> processes the
                        <code>workspace/applyEdit</code> payload, opening and
                        modifying files as needed.` },
    { vscode: `Preserves undo history`, emacs: `Native Emacs undo handles the multi-file edits cleanly.` },
    { vscode: `Fails gracefully on invalid targets`, emacs: `<code>eglot</code> displays a user-friendly error if
                        <code>prepareRename</code> rejects the location (e.g.,
                        renaming a keyword).` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Rename symbol", cmd: "eglot-rename", key: `<kbd>F2</kbd> / <kbd>SPC c r</kbd>`, notes: "Prompts for new name and applies project-wide rename." },
    { action: "Prepare rename (internal)", cmd: "eglot--prepare-rename", key: `—`, notes: "Automatically invoked byeglot-renameto
                        check if the symbol is valid for renaming." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

