---
title: "Peek Definition / Peek References"
category: "Navigation & Code Jumping"
slug: "peek-definition"
status: "working"
vscode_parity:
  - "Inline expandable panel showing definition or references without\n            tab switching (Alt+F12 / Shift+Alt+F12)"
lsp_methods:
  - "textDocument/definition"
  - "textDocument/references"
routing: "eglot→xref→peek (overlay rendering)"
commands:
  - action: "Peek definition"
    cmd: "peek-xref-definition"
    key: "<kbd>SPC c p d</kbd>"
    notes: "Shows the definition inline without leaving the current\n                        buffer."
  - action: "Peek references"
    cmd: "peek-xref-references"
    key: "<kbd>SPC c p r</kbd>"
    notes: "Custom wrapper to show the first reference inline."
  - action: "Hide peek view"
    cmd: "peek-overlay-dwim"
    key: "<kbd>SPC c p h</kbd>"
    notes: "Toggles or hides the active peek overlay."
  - action: "Standard jump (fallback)"
    cmd: "xref-find-definitions"
    key: "<kbd>M-.</kbd> / <kbd>g d</kbd>"
    notes: "Jumps to the target in a new buffer if inline peek is\n                        not desired."
parity_matrix:
  - vscode: "<kbd>Alt+F12</kbd> peeks definition inline"
    emacs: "<kbd>SPC c p d</kbd> (<code>peek-xref-definition</code>)\n                        renders the target below the cursor."
  - vscode: "<kbd>Shift+Alt+F12</kbd> peeks references"
    emacs: "<kbd>SPC c p r</kbd> (<code>peek-xref-references</code>)\n                        renders the first reference inline."
  - vscode: "Expandable panel without tab switch"
    emacs: "<code>peek</code> uses buffer overlays, keeping the\n                        original buffer fully visible and active."
  - vscode: "Scroll within the peek panel"
    emacs: "Native Emacs scrolling (<kbd>C-n</kbd> / <kbd>C-p</kbd>\n                        or arrow keys) works within the overlay context."
  - vscode: "Close peek panel"
    emacs: "<kbd>SPC c p h</kbd> (<code>peek-overlay-dwim</code>)\n                        hides the overlay instantly."
  - vscode: "Live update on source change"
    emacs: "<code>peek-live-update t</code> refreshes the overlay if\n                        the underlying code is modified."
config_filename: "init-peek.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>Alt+F12</kbd> peeks definition inline`, emacs: `<kbd>SPC c p d</kbd> (<code>peek-xref-definition</code>)
                        renders the target below the cursor.` },
    { vscode: `<kbd>Shift+Alt+F12</kbd> peeks references`, emacs: `<kbd>SPC c p r</kbd> (<code>peek-xref-references</code>)
                        renders the first reference inline.` },
    { vscode: `Expandable panel without tab switch`, emacs: `<code>peek</code> uses buffer overlays, keeping the
                        original buffer fully visible and active.` },
    { vscode: `Scroll within the peek panel`, emacs: `Native Emacs scrolling (<kbd>C-n</kbd> / <kbd>C-p</kbd>
                        or arrow keys) works within the overlay context.` },
    { vscode: `Close peek panel`, emacs: `<kbd>SPC c p h</kbd> (<code>peek-overlay-dwim</code>)
                        hides the overlay instantly.` },
    { vscode: `Live update on source change`, emacs: `<code>peek-live-update t</code> refreshes the overlay if
                        the underlying code is modified.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Peek definition", cmd: "peek-xref-definition", key: `<kbd>SPC c p d</kbd>`, notes: "Shows the definition inline without leaving the current
                        buffer." },
    { action: "Peek references", cmd: "peek-xref-references", key: `<kbd>SPC c p r</kbd>`, notes: "Custom wrapper to show the first reference inline." },
    { action: "Hide peek view", cmd: "peek-overlay-dwim", key: `<kbd>SPC c p h</kbd>`, notes: "Toggles or hides the active peek overlay." },
    { action: "Standard jump (fallback)", cmd: "xref-find-definitions", key: `<kbd>M-.</kbd> / <kbd>g d</kbd>`, notes: "Jumps to the target in a new buffer if inline peek is
                        not desired." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

