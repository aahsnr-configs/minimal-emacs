---
title: "Call Hierarchy"
category: "Navigation & Code Jumping"
slug: "call-hierarchy"
status: "working"
vscode_parity:
  - "\"Show Call Hierarchy\" (incoming/outgoing calls tree view)"
lsp_methods:
  - "textDocument/prepareCallHierarchy"
  - "callHierarchy/incomingCalls"
  - "callHierarchy/outgoingCalls"
routing: "eglot→eglot-show-call-hierarchy→interactive *eglot-hierarchy* buffer"
commands:
  - action: "Show call hierarchy"
    cmd: "eglot-show-call-hierarchy"
    key: "<kbd>SPC c h</kbd> / <kbd>g c</kbd>"
    notes: "Opens the interactive caller/callee tree for the symbol\n                        at point."
  - action: "Show type hierarchy"
    cmd: "eglot-show-type-hierarchy"
    key: "<kbd>SPC c t</kbd> / <kbd>g T</kbd>"
    notes: "Opens the interactive supertype/subtype tree for the\n                        symbol at point."
  - action: "Center on node"
    cmd: "eglot-hierarchy-center-on-node"
    key: "<kbd>c</kbd> (in hierarchy buffer)"
    notes: "Recenters the tree view on the current node."
  - action: "Expand/Collapse node"
    cmd: "tabulated-listnative"
    key: "<kbd>TAB</kbd> / <kbd>RET</kbd>"
    notes: "Toggles the visibility of child nodes in the tree."
  - action: "Jump to definition"
    cmd: "xref-find-definitions"
    key: "<kbd>RET</kbd> (on node)"
    notes: "Jumps to the selected caller/callee definition in the\n                        source buffer."
parity_matrix:
  - vscode: "<kbd>Shift+Alt+H</kbd> opens call hierarchy"
    emacs: "<kbd>SPC c h</kbd> or <kbd>g c</kbd> invokes\n                        <code>eglot-show-call-hierarchy</code>."
  - vscode: "Tree view of incoming calls (callers)"
    emacs: "Native Eglot hierarchy buffer displays \"Incoming Calls\"\n                        expandable nodes."
  - vscode: "Tree view of outgoing calls (callees)"
    emacs: "Native Eglot hierarchy buffer displays \"Outgoing Calls\"\n                        expandable nodes."
  - vscode: "Click node to jump to source"
    emacs: "<kbd>RET</kbd> on a hierarchy node triggers\n                        <code>xref-find-definitions</code> to jump to the\n                        location."
  - vscode: "Expand/collapse tree branches"
    emacs: "<kbd>TAB</kbd> or <kbd>RET</kbd> on parent nodes toggles\n                        child visibility."
  - vscode: "Center view on current symbol"
    emacs: "<code>eglot-hierarchy-center-on-node</code> recenters\n                        the tree on the active symbol."
config_filename: "init-call-hierarchy.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>Shift+Alt+H</kbd> opens call hierarchy`, emacs: `<kbd>SPC c h</kbd> or <kbd>g c</kbd> invokes
                        <code>eglot-show-call-hierarchy</code>.` },
    { vscode: `Tree view of incoming calls (callers)`, emacs: `Native Eglot hierarchy buffer displays "Incoming Calls"
                        expandable nodes.` },
    { vscode: `Tree view of outgoing calls (callees)`, emacs: `Native Eglot hierarchy buffer displays "Outgoing Calls"
                        expandable nodes.` },
    { vscode: `Click node to jump to source`, emacs: `<kbd>RET</kbd> on a hierarchy node triggers
                        <code>xref-find-definitions</code> to jump to the
                        location.` },
    { vscode: `Expand/collapse tree branches`, emacs: `<kbd>TAB</kbd> or <kbd>RET</kbd> on parent nodes toggles
                        child visibility.` },
    { vscode: `Center view on current symbol`, emacs: `<code>eglot-hierarchy-center-on-node</code> recenters
                        the tree on the active symbol.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Show call hierarchy", cmd: "eglot-show-call-hierarchy", key: `<kbd>SPC c h</kbd> / <kbd>g c</kbd>`, notes: "Opens the interactive caller/callee tree for the symbol
                        at point." },
    { action: "Show type hierarchy", cmd: "eglot-show-type-hierarchy", key: `<kbd>SPC c t</kbd> / <kbd>g T</kbd>`, notes: "Opens the interactive supertype/subtype tree for the
                        symbol at point." },
    { action: "Center on node", cmd: "eglot-hierarchy-center-on-node", key: `<kbd>c</kbd> (in hierarchy buffer)`, notes: "Recenters the tree view on the current node." },
    { action: "Expand/Collapse node", cmd: "tabulated-listnative", key: `<kbd>TAB</kbd> / <kbd>RET</kbd>`, notes: "Toggles the visibility of child nodes in the tree." },
    { action: "Jump to definition", cmd: "xref-find-definitions", key: `<kbd>RET</kbd> (on node)`, notes: "Jumps to the selected caller/callee definition in the
                        source buffer." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

