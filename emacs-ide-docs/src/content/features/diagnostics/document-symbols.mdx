---
title: "Document Symbols / Outline View"
category: "Diagnostics & Symbols"
slug: "document-symbols"
status: "working"
vscode_parity:
  - "Outline sidebar, Ctrl+Shift+O (Go to Symbol in File), and top\n            breadcrumb bar"
lsp_methods:
  - "textDocument/documentSymbol"
routing: "eglot→imenu→consult-eglot-symbols OR breadcrumb"
commands:
  - action: "Go to symbol in file"
    cmd: "consult-eglot-symbols"
    key: "<kbd>SPC c s</kbd>"
    notes: "Opens a vertico-powered, preview-enabled outline\n                        dropdown."
  - action: "Go to symbol in workspace"
    cmd: "consult-eglot-symbols(withC-u)"
    key: "<kbd>C-u SPC c s</kbd>"
    notes: "Searches across the entire project viaworkspace/symbol."
  - action: "Toggle breadcrumbs"
    cmd: "breadcrumb-mode"
    key: "<kbd>SPC t b</kbd>"
    notes: "Enables the clickable path bar at the top of the buffer."
  - action: "Native imenu jump"
    cmd: "imenu"
    key: "<kbd>M-g M-i</kbd>"
    notes: "Fallback to native Emacs imenu if LSP is unavailable."
parity_matrix:
  - vscode: "<kbd>Ctrl+Shift+O</kbd> opens file outline"
    emacs: "<kbd>SPC c s</kbd> (<code>consult-eglot-symbols</code>)\n                        opens minibuffer outline with live preview."
  - vscode: "Fuzzy filter symbols by name"
    emacs: "<code>consult</code> + <code>orderless</code> allows\n                        space-separated, out-of-order fuzzy matching."
  - vscode: "Click symbol to jump to definition"
    emacs: "<kbd>RET</kbd> in <code>consult</code> dropdown or\n                        <kbd>mouse-1</kbd> on breadcrumb segments."
  - vscode: "Top bar shows <code>file › class › method</code>"
    emacs: "<code>breadcrumb-mode</code> renders this exact\n                        hierarchy in the header line."
  - vscode: "Workspace-wide symbol search (<kbd>Ctrl+T</kbd>)"
    emacs: "<kbd>C-u SPC c s</kbd> or <kbd>SPC s w</kbd> triggers\n                        <code>workspace/symbol</code> via <code>consult</code>."
  - vscode: "Icons for classes/functions in outline"
    emacs: "<code>nerd-icons-completion</code> automatically injects\n                        glyphs into the <code>consult</code> dropdown."
config_filename: "init-symbols.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>Ctrl+Shift+O</kbd> opens file outline`, emacs: `<kbd>SPC c s</kbd> (<code>consult-eglot-symbols</code>)
                        opens minibuffer outline with live preview.` },
    { vscode: `Fuzzy filter symbols by name`, emacs: `<code>consult</code> + <code>orderless</code> allows
                        space-separated, out-of-order fuzzy matching.` },
    { vscode: `Click symbol to jump to definition`, emacs: `<kbd>RET</kbd> in <code>consult</code> dropdown or
                        <kbd>mouse-1</kbd> on breadcrumb segments.` },
    { vscode: `Top bar shows <code>file › class › method</code>`, emacs: `<code>breadcrumb-mode</code> renders this exact
                        hierarchy in the header line.` },
    { vscode: `Workspace-wide symbol search (<kbd>Ctrl+T</kbd>)`, emacs: `<kbd>C-u SPC c s</kbd> or <kbd>SPC s w</kbd> triggers
                        <code>workspace/symbol</code> via <code>consult</code>.` },
    { vscode: `Icons for classes/functions in outline`, emacs: `<code>nerd-icons-completion</code> automatically injects
                        glyphs into the <code>consult</code> dropdown.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Go to symbol in file", cmd: "consult-eglot-symbols", key: `<kbd>SPC c s</kbd>`, notes: "Opens a vertico-powered, preview-enabled outline
                        dropdown." },
    { action: "Go to symbol in workspace", cmd: "consult-eglot-symbols(withC-u)", key: `<kbd>C-u SPC c s</kbd>`, notes: "Searches across the entire project viaworkspace/symbol." },
    { action: "Toggle breadcrumbs", cmd: "breadcrumb-mode", key: `<kbd>SPC t b</kbd>`, notes: "Enables the clickable path bar at the top of the buffer." },
    { action: "Native imenu jump", cmd: "imenu", key: `<kbd>M-g M-i</kbd>`, notes: "Fallback to native Emacs imenu if LSP is unavailable." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

