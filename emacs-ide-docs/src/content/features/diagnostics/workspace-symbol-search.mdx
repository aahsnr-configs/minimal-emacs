---
title: "Workspace Symbol Search"
category: "Diagnostics & Symbols"
slug: "workspace-symbol-search"
status: "working"
vscode_parity:
  - "Ctrl+T \"Go to Symbol in Workspace\" — fuzzy search symbols across\n            the whole project"
lsp_methods:
  - "workspace/symbol"
routing: "eglot→consult-eglot-symbols→vertico + orderless"
commands:
  - action: "Workspace symbol search"
    cmd: "consult-eglot-symbols"
    key: "<kbd>C-u SPC s w</kbd>"
    notes: "Prefix argument (C-u) forces workspace-wide\n                        search instead of buffer-local."
  - action: "Buffer-local symbol search"
    cmd: "consult-eglot-symbols"
    key: "<kbd>SPC c s</kbd>"
    notes: "Default behavior (no prefix) searches only the current\n                        file viatextDocument/documentSymbol."
  - action: "Built-in workspace search"
    cmd: "eglot-workspace-symbols"
    key: "<kbd>M-x eglot-workspace-symbols</kbd>"
    notes: "Fallback native command without live preview."
  - action: "Apropos search (fallback)"
    cmd: "xref-find-apropos"
    key: "<kbd>SPC c A</kbd>"
    notes: "Searches all registered xref backends (including eglot)\n                        for a regex pattern."
parity_matrix:
  - vscode: "<kbd>Ctrl+T</kbd> opens workspace symbol search"
    emacs: "<kbd>C-u SPC s w</kbd> or <kbd>C-u M-g S</kbd> triggers\n                        workspace-wide <code>consult-eglot-symbols</code>."
  - vscode: "Fuzzy filter symbols by name"
    emacs: "<code>orderless</code> matching styles allow\n                        space-separated, out-of-order fuzzy matching."
  - vscode: "Live preview of symbol definition"
    emacs: "<code>consult</code> temporarily visits the file and\n                        shows the definition context in a side window."
  - vscode: "Filter by symbol kind (class, function)"
    emacs: "<code>consult-eglot-symbols-kind</code> can be set to\n                        filter specific LSP symbol kinds."
  - vscode: "Click/Enter to jump to target"
    emacs: "<kbd>RET</kbd> in <code>consult</code> dropdown jumps to\n                        the exact location via <code>xref</code>."
  - vscode: "Icons for classes/functions in list"
    emacs: "<code>nerd-icons-completion</code> automatically injects\n                        glyphs into the <code>consult</code> dropdown."
config_filename: "init-workspace-symbols.el"
---

import { Accordion, ParityMatrix, StackGrid, EcoGrid, VsCard, EnhCard, CodeWindow, CommandTable } from '../../components';

<Accordion title="Feature Overview" icon="grid" id="feature-overview" defaultOpen>
  <ParityMatrix items={[
    { vscode: `<kbd>Ctrl+T</kbd> opens workspace symbol search`, emacs: `<kbd>C-u SPC s w</kbd> or <kbd>C-u M-g S</kbd> triggers
                        workspace-wide <code>consult-eglot-symbols</code>.` },
    { vscode: `Fuzzy filter symbols by name`, emacs: `<code>orderless</code> matching styles allow
                        space-separated, out-of-order fuzzy matching.` },
    { vscode: `Live preview of symbol definition`, emacs: `<code>consult</code> temporarily visits the file and
                        shows the definition context in a side window.` },
    { vscode: `Filter by symbol kind (class, function)`, emacs: `<code>consult-eglot-symbols-kind</code> can be set to
                        filter specific LSP symbol kinds.` },
    { vscode: `Click/Enter to jump to target`, emacs: `<kbd>RET</kbd> in <code>consult</code> dropdown jumps to
                        the exact location via <code>xref</code>.` },
    { vscode: `Icons for classes/functions in list`, emacs: `<code>nerd-icons-completion</code> automatically injects
                        glyphs into the <code>consult</code> dropdown.` },
  ]} />
</Accordion>

<Accordion title="Ecosystem Integration" icon="globe" id="ecosystem-integration">
</Accordion>

<Accordion title="Implementation Stack" icon="stack" id="implementation-stack">
</Accordion>

<Accordion title="Commands &amp; Keybindings" icon="keyboard" id="commands-keybindings">
  <CommandTable items={[
    { action: "Workspace symbol search", cmd: "consult-eglot-symbols", key: `<kbd>C-u SPC s w</kbd>`, notes: "Prefix argument (C-u) forces workspace-wide
                        search instead of buffer-local." },
    { action: "Buffer-local symbol search", cmd: "consult-eglot-symbols", key: `<kbd>SPC c s</kbd>`, notes: "Default behavior (no prefix) searches only the current
                        file viatextDocument/documentSymbol." },
    { action: "Built-in workspace search", cmd: "eglot-workspace-symbols", key: `<kbd>M-x eglot-workspace-symbols</kbd>`, notes: "Fallback native command without live preview." },
    { action: "Apropos search (fallback)", cmd: "xref-find-apropos", key: `<kbd>SPC c A</kbd>`, notes: "Searches all registered xref backends (including eglot)
                        for a regex pattern." },
  ]} />
</Accordion>

<Accordion title="Configuration" icon="code" id="configuration">
</Accordion>

<Accordion title="Architecture &amp; Enhancements" icon="book" id="architecture-enhancements">
</Accordion>

