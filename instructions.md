You are tasked with revising and rewrite a custom vanilla emacs configuration across multiple chats. You will have 5 files uploaded in each chat that you must ingest. You are to strictly follow all the instructions laid out on the system_prompt_protocol.yaml file. The config.org.txt file contains the current version of my full custom vanilla emacs configuration. You will keep track of the progress of this configuration as it gets updated by keeping in context the subsections that get finalized. You will update and rewrite the project_state.yaml file based on the progress of the emacs configuration. You will assign a numbered value to the iteration number determined by how many times a particular subsection has been rewritten. Finally, you will use the use_package.yaml for the common rules of how a use-package configuration works.
