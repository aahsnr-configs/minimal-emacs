# Language Server Protocol

---

## lsp-mode

| Variable                           | Subsystem / Category   | Technical Description                                                                                                 | Default Value                              |
| ---------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `lsp-keymap-prefix`                | Workspace & Lifecycle  | The keyboard modifier prefix sequence that anchors all automatically bound interactive commands.                      | `"s-l"`                                    |
| `lsp-auto-configure`               | Workspace & Lifecycle  | Master switch determining if default clients, hooks, and UI companion extensions load automatically.                  | `t`                                        |
| `lsp-auto-guess-root`              | Workspace & Lifecycle  | Uses project boundaries (`.git`, `project.el`) to target the root directory without interactive confirmation prompts. | `nil`                                      |
| `lsp-keep-workspace-alive`         | Workspace & Lifecycle  | Controls whether the background language server process is preserved when its last buffer closes.                     | `t`                                        |
| `lsp-session-file`                 | Workspace & Lifecycle  | File path where persistent workspace metadata, blacklisted paths, and folder mappings are stored.                     | `(locate-user-emacs-file ".lsp-session")`  |
| `lsp-server-install-dir`           | Workspace & Lifecycle  | Isolated internal directory where automated language server binary downloads are placed.                              | `(locate-user-emacs-file ".lsp-servers/")` |
| `lsp-idle-delay`                   | Performance & I/O      | Debounce interval (in seconds) Emacs waits during idle time before running background document analyses.              | `0.5`                                      |
| `lsp-response-timeout`             | Performance & I/O      | Maximum time (in seconds) to wait for synchronous server replies before dropping requests to prevent hangs.           | `10`                                       |
| `lsp-log-io`                       | Performance & I/O      | Records raw JSON-RPC inbound/outbound strings to a dedicated buffer (causes heavy editor lag if enabled).             | `nil`                                      |
| `lsp-enable-file-watchers`         | Performance & I/O      | Toggles internal file system event hooks tracking structural project shifts externally.                               | `t`                                        |
| `lsp-file-watchers-max-num-files`  | Performance & I/O      | Safety cap of indexed project files allowed before halting file-watching infrastructure to save memory.               | `3000`                                     |
| `lsp-use-plists`                   | Performance & I/O      | Switches JSON deserialization arrays from hash tables to plists for native compilation performance optimization.      | `nil`                                      |
| `lsp-completion-provider`          | Integration & Features | Selector for processing autocompletion; setting to `:none` shifts delegation to frontends like `corfu`.               | `:capf`                                    |
| `lsp-diagnostics-provider`         | Integration & Features | Directs syntax validation engines to route structured code diagnostic outputs via `:flycheck` or `:flymake`.          | `:auto`                                    |
| `lsp-enable-xref`                  | Integration & Features | Instructs Emacs to override native cross-referencing (`M-.`) with server-side definition tracking.                    | `t`                                        |
| `lsp-enable-indentation`           | Integration & Features | Defers buffer-wide indentation rules and alignments completely to server configuration rules.                         | `t`                                        |
| `lsp-enable-on-type-formatting`    | Integration & Features | Executes instantaneous block formatting as structural characters (like `;` or `}`) are added.                         | `t`                                        |
| `lsp-format-buffer-on-save`        | Integration & Features | Triggers an automated, document-wide code layout normalization block instantly upon saving files.                     | `nil`                                      |
| `lsp-headerline-breadcrumb-enable` | Visual & UI Overlays   | Renders a structural file scope and symbol path indicator along the top edge of the window frame.                     | `t`                                        |
| `lsp-lens-enable`                  | Visual & UI Overlays   | Renders non-interactive, virtual code reference counters directly above method declarations.                          | `t`                                        |
| `lsp-modeline-diagnostics-enable`  | Visual & UI Overlays   | Embeds live project-wide or local-buffer error and warning diagnostic totals into the active modeline.                | `t`                                        |
| `lsp-signature-auto-activate`      | Visual & UI Overlays   | Automatically displays function parameter tooltips when contextually editing parameter regions.                       | `t`                                        |

---

## lsp-ui

| Variable Name                              | Subsystem / Category | Technical Description                                                                                                                                                    | Default Value                 |
| ------------------------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------- |
| **`lsp-ui-remap-xref-keybindings`**        | General / Core       | Remaps the core standard cross-reference definition and reference lookup keys (`M-.` and `M-?`) to automatically invoke the rich `lsp-ui-peek` panel variants.           | `nil`                         |
| **`lsp-ui-doc-enable`**                    | `lsp-ui-doc`         | Master boolean switch to globally toggle the rendering of hover documentation popups.                                                                                    | `t`                           |
| **`lsp-ui-doc-show-with-cursor`**          | `lsp-ui-doc`         | Triggers the item documentation popup when the editing point (cursor) sits directly over a valid language symbol.                                                        | `nil`                         |
| **`lsp-ui-doc-show-with-mouse`**           | `lsp-ui-doc`         | Triggers the item documentation popup window when the mouse cursor hovers over a valid code token.                                                                       | `t`                           |
| **`lsp-ui-doc-delay`**                     | `lsp-ui-doc`         | Specifies the exact amount of idle time in seconds to wait before requesting and constructing the documentation frame overlay.                                           | `0.2`                         |
| **`lsp-ui-doc-position`**                  | `lsp-ui-doc`         | Configures the anchor placement coordinates of the documentation pane frame relative to the text. Options include `'top`, `'bottom`, or `'at-point`.                     | `'at-point`                   |
| **`lsp-ui-doc-side`**                      | `lsp-ui-doc`         | Specifies the target horizontal layout boundary side to draw the popover text block frame. Options include `'left`, `'right`, or `'other`.                               | `'right`                      |
| **`lsp-ui-doc-alignment`**                 | `lsp-ui-doc`         | Selects the alignment coordinate reference context frame for structural evaluation. Options include `'frame` or `'window`.                                               | `'window`                     |
| **`lsp-ui-doc-border`**                    | `lsp-ui-doc`         | Explicitly sets the frame line hex or system color assigned to render the outer boundary line edge of the childframe viewport window.                                    | `"white"`                     |
| **`lsp-ui-doc-max-width`**                 | `lsp-ui-doc`         | Defines the maximum layout horizontal boundary text width constraint metric (in characters) before wrapping data contents.                                               | `150`                         |
| **`lsp-ui-doc-max-height`**                | `lsp-ui-doc`         | Defines the maximum layout vertical boundary block row sizing limit applied to truncate excessively verbose documentation texts.                                         | `30`                          |
| **`lsp-ui-doc-use-childframe`**            | `lsp-ui-doc`         | Dictates if Emacs should employ graphical childframes (`posframe`) to isolate and render the UI documentation popup overlay cleanly over syntax buffers.                 | `t`                           |
| **`lsp-ui-doc-use-webkit`**                | `lsp-ui-doc`         | Performance impact flag. If enabled (`t`), renders incoming markdown strings via an embedded WebKit frame widget. _Requires Emacs built `--with-xwidgets`._              | `nil`                         |
| **`lsp-ui-doc-include-signature`**         | `lsp-ui-doc`         | Directs the layout engine to explicitly parse and format the formal object structural signature template block inside the top of the documentation layout window.        | `t`                           |
| **`lsp-ui-doc-winum-ignore`**              | `lsp-ui-doc`         | Forces window navigation layout frameworks like `winum-mode` to ignore indexing the transient overlay childframes to keep main window numbers untouched.                 | `t`                           |
| **`lsp-ui-doc-render-function`**           | `lsp-ui-doc`         | Anchor hook parameter containing a custom functional parsing routine for styling hover strings. If left empty, text defaults straight to native markdown conversion.     | `nil`                         |
| **`lsp-ui-sideline-enable`**               | `lsp-ui-sideline`    | Master toggle flag configuration setting that handles the active rendering of the inline context sideline pane column strip.                                             | `t`                           |
| **`lsp-ui-sideline-show-diagnostics`**     | `lsp-ui-sideline`    | Projects diagnostic linter messages, compiler errors, and warning strings directly into the right-hand margin workspace strip space.                                     | `t`                           |
| **`lsp-ui-sideline-show-hover`**           | `lsp-ui-sideline`    | Grabs contextual documentation snippet references and object type structures for active points and formats them into the margin sideline panel space.                    | `t`                           |
| **`lsp-ui-sideline-show-code-actions`**    | `lsp-ui-sideline`    | Evaluates background server requests to show actionable quick-fix prompts and lightbulb helpers inline inside the sideline layer.                                        | `t`                           |
| **`lsp-ui-sideline-update-mode`**          | `lsp-ui-sideline`    | Determines execution interval granularity. `'line` recomputes layout variables only upon changing line numbers; `'point` refreshes dynamically on every movement.        | `'line`                       |
| **`lsp-ui-sideline-delay`**                | `lsp-ui-sideline`    | Performance flag determining the duration threshold parameter in seconds to hold back computing and updating the side-column text overlays.                              | `0.2`                         |
| **`lsp-ui-sideline-diagnostic-max-lines`** | `lsp-ui-sideline`    | Restricts how many text rows an inline linter or compiler diagnostic warning string description block may occupy inside the sideline container.                          | `1`                           |
| **`lsp-ui-sideline-ignore-duplicate`**     | `lsp-ui-sideline`    | Suppresses redundant diagnostic error logs from flooding the sideline layer when multiple language server backends yield duplicate reporting blocks.                     | `t`                           |
| **`lsp-ui-sideline-show-symbol`**          | `lsp-ui-sideline`    | Appends the formal target name identifier literal configuration alongside its diagnostic mapping records inside the margin block space.                                  | `nil`                         |
| **`lsp-ui-peek-enable`**                   | `lsp-ui-peek`        | Activates the advanced split-screen overlay layout interface framework that replaces standard code navigation and cross-reference buffers.                               | `t`                           |
| **`lsp-ui-peek-show-directory`**           | `lsp-ui-peek`        | Toggles whether to output structural system file directories alongside source file tags inside the references listing column interface.                                  | `t`                           |
| **`lsp-ui-peek-always-show`**              | `lsp-ui-peek`        | Forces the split-pane peek navigation portal to deploy open even when the language server only finds exactly one cross-reference result.                                 | `nil`                         |
| **`lsp-ui-peek-list-width`**               | `lsp-ui-peek`        | Controls the column layout ratio sizing metrics by declaring the character text formatting width assigned strictly to the cross-reference list menu sidebar.             | `60`                          |
| **`lsp-ui-peek-peek-height`**              | `lsp-ui-peek`        | Declares the vertical row height allocated to draw the code preview viewport windows panel inside the inline peek portal box framework.                                  | `20`                          |
| **`lsp-ui-peek-fontify`**                  | `lsp-ui-peek`        | Performance flag controlling text fontification options inside the peek code block frame interface. Options include `'always`, `'never`, or `'on-demand`.                | `'always`                     |
| **`lsp-ui-peek-expand-function`**          | `lsp-ui-peek`        | Directs the UI mechanism via a programmatic function mapping hook to dictate exactly which source files start unfolded or collapsed in search listings.                  | `'lsp-ui-peek--expand-buffer` |
| **`lsp-ui-imenu-enable`**                  | `lsp-ui-imenu`       | Toggles the sidebar minor-mode view window block overlay framework specialized for displaying structural workspace symbol tables.                                        | `t`                           |
| **`lsp-ui-imenu-kind-position`**           | `lsp-ui-imenu`       | Declares the structural positioning layout location parameter for drawing kind definition indicator tags. Options include `'left`, `'right`, or `'top`.                  | `'left`                       |
| **`lsp-ui-imenu-buffer-position`**         | `lsp-ui-imenu`       | Selects which edge of the framework layout display window column anchor to lock the symbol outline tree buffer. Options include `'left`, `'right`, `'top`, or `'bottom`. | `'left`                       |
| **`lsp-ui-imenu-window-width`**            | `lsp-ui-imenu`       | Establishes the static total character line text formatting width metric used to resize the document symbol index browsing pane panel split.                             | `30`                          |
| **`lsp-ui-imenu-window-fix-width`**        | `lsp-ui-imenu`       | Prevents standard window resizing commands like `balance-windows` from modifying or layout-scaling the width dimension constraints of the imenu frame.                   | `nil`                         |
| **`lsp-ui-imenu-auto-refresh`**            | `lsp-ui-imenu`       | Sets automation behaviors for structural validation syncing. Options include `t`, `'after-save`, or `nil` (requires explicit key refresh triggers).                      | `nil`                         |
| **`lsp-ui-imenu-refresh-delay`**           | `lsp-ui-imenu`       | Performance delay timer tracking idle execution intervals in seconds before parsing files and rendering new tree updates to avoid stuttering inputs.                     | `0.5`                         |
| **`lsp-ui-imenu-colors`**                  | `lsp-ui-imenu`       | Lists hexadecimal colors or text theme color variables assigned systematically to differentiate indentation levels of structural code outlines.                          | _(List of hex color strings)_ |
| **`lsp-ui-flycheck-enable`**               | `lsp-ui-flycheck`    | Connects workspace diagnostics straight into a dedicated, interactive `lsp-ui` diagnostic navigation buffer pane layout framework.                                       | `t`                           |
| **`lsp-ui-flycheck-list-position`**        | `lsp-ui-flycheck`    | Chooses layout boundaries context to lock and anchor the target output diagnostic list buffer layout container. Options include `'left`, `'right`, `'top`, or `'bottom`. | `'right`                      |
| **`lsp-ui-flycheck-live-reporting`**       | `lsp-ui-flycheck`    | Governs update dispatchers. If enabled (`t`), it continuously pushes diagnostics updates instantly as keys are struck vs waiting for explicit disk file write actions.   | `t`                           |

---

## dap-mode

| Variable Name                          | Subsystem / Category      | Technical Description                                                                                                                                                                                                                          | Default Value                                                  |
| -------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| **`dap-auto-configure-mode`**          | General & Lifecycle       | Global minor mode configuration switch that automatically initializes standard debugging views, hooks language-specific debug providers, and manages frame rendering pipelines upon project session entry.                                     | `nil`                                                          |
| **`dap-auto-configure-features`**      | General & Lifecycle       | An ordered list of atomic IDE features automatically spawned by `dap-auto-configure-mode`. Valid elements inside the list block include `'sessions`, `'locals`, `'breakpoints`, `'expressions`, `'controls`, and `'tooltip`.                   | `'(sessions locals breakpoints expressions controls tooltip)`  |
| **`dap-enable-mouse-support`**         | General & Lifecycle       | Toggles comprehensive visual mouse pointer listeners inside execution buffers, allowing users to hover over variables for tooltips and tap marginal layout regions to toggle code execution nodes.                                             | `t`                                                            |
| **`dap-utils-extension-path`**         | General & Lifecycle       | The fundamental system directory mapping point where `dap-mode` automatically isolates, unpacks, tracks, and invokes external downloaded VS Code `.vsix` structural adapter frameworks.                                                        | `(expand-file-name ".extension/vscode/" user-emacs-directory)` |
| **`dap-print-io`**                     | Performance & Diagnostics | Performance impact flag. When toggled to `t`, it causes the background client process to synchronously print every single raw inbound and outbound JSON-RPC transport string into the `*Messages*` pipeline (causes serious input stuttering). | `nil`                                                          |
| **`dap-log-message-level`**            | Performance & Diagnostics | Dictates the lowest log level constraint filtered through internal event streaming pipelines. Valid settings match basic level abstractions, primarily `'debug`, `'info`, `'warn`, or `'error`.                                                | `'info`                                                        |
| **`dap-report-resetted-associations`** | Performance & Diagnostics | Governs whether Emacs should output notification reports when runtime structural mappings between standard editor source files and the server-side debugging thread contexts clear out or reset.                                               | `t`                                                            |
| **`dap-ui-buffer-configurations`**     | UI Window & Layout        | Association list determining layout geometries, scaling ratios, anchoring locations, and internal grouping variables for the structural diagnostic windows framework (e.g., Locals, Stacktrace, Sessions).                                     | _(Complex internal window layout alist)_                       |
| **`dap-ui-locals-expand-depth`**       | UI Window & Layout        | Specifies tree-nesting depth bounds when analyzing variable structures inside the local variables watch window. Can be a discrete integer level, `t` for absolute recursive discovery, or `nil` to suppress expansion.                         | `nil`                                                          |
| **`dap-ui-sessions-expand-depth`**     | UI Window & Layout        | Declares structural parsing limits applied to active execution threads and server execution runtimes within the active session management sidebar tracker component tree.                                                                      | `nil`                                                          |
| **`dap-ui-overlay-priority`**          | UI Window & Layout        | Sets baseline rendering queue numbers assigned to graphical text slices and fringe breakpoint indicator blocks to ensure priority over generic lint highlights or search results.                                                              | `100`                                                          |
| **`dap-ui-controls-screen-position`**  | UI Window & Layout        | Coordinates alignment properties for rendering the persistent visual execution layout controls floating widget frame (e.g., step-over, pause, stop buttons) atop text areas.                                                                   | `((:top . 0) (:right . 0))`                                    |
| **`dap-ui-variable-length`**           | UI Window & Layout        | Structural safety delimiter configuration setting that forces the variable data parser to clip and ellipse string values beyond a given character threshold boundary to avoid lagging window redraw cycles.                                    | `100`                                                          |
| **`dap-auto-show-output`**             | Breakpoint & Execution    | Rules handling active buffer focusing changes. Setting values specify if stdout/stderr output frame buffers forcefully emerge into view. Valid choices include `'always`, `'never`, or `'hide-on-stop`.                                        | `'always`                                                      |
| **`dap-output-window-max-lines`**      | Breakpoint & Execution    | Defines the maximum layout retention scrollback buffer capacity bound assigned to external server execution streams before cleaning up early telemetry contents.                                                                               | `5000`                                                         |
| **`dap-overlays-use-blinking`**        | Breakpoint & Execution    | Dictates whether graphical instruction pointers and line execution tracking banners utilize blinking highlights at evaluation target nodes to capture user attention.                                                                          | `nil`                                                          |
| **`dap-ui-menu-show-gdb-layout`**      | UI Window & Layout        | Toggles if layout macro triggers adapt structural panes to mimic traditional multi-window GDB execution designs when stepping into binary applications.                                                                                        | `nil`                                                          |
| **`dap-python-debugger`**              | Language Integration      | Dictates the specific default engine adapter used for Python threads context mapping tasks. Options are typically restricted to downstream providers: `'debugpy` or older legacy `'ptvsd`.                                                     | `'debugpy`                                                     |
| **`dap-python-executable`**            | Language Integration      | Establishes the binary name target path or executable name evaluated in project environments to initialize structural backend server environments.                                                                                             | `"python"`                                                     |
| **`dap-gdb-debug-program`**            | Language Integration      | A structural vector containing executable launch commands and native instruction inputs parsed by Emacs to call the host debugger subsystem engine.                                                                                            | `("gdb" "-i" "dap")`                                           |
| **`dap-codelldb-debug-program`**       | Language Integration      | Path mapping entry tracking compiled locations of the automated native execution engine binary adapter distributed via the CodeLLDB workspace pipeline.                                                                                        | `n/a` _(Computed dynamically based on system OS)_              |
| **`dap-go-delve-path`**                | Language Integration      | Direct path pointer variable indicating the global system address or workspace directory location for structural execution of the Delve (`dlv`) debugging application.                                                                         | `(or (executable-find "dlv") ...)`                             |
